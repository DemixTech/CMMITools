{"complexity": 7, "estTokens": 320000, "model": "claude-opus-4-8", "estSeconds": 1200, "notes": "Wide but mechanical refactor across a ~3,400-line const-heavy Main.cs: extract one user-specific path into App.config/typed options and consolidate dozens of c*Col/c*Row/cMostPA*/cIIandGOV* constants into a WorkbookLayout class, then rewire every reference. Token cost is driven by reading the large file plus multiple edit passes; complexity is raised by regression risk if any constant is missed or remapped. Key risk: the 'smoke run against a sample appraisal folder' Done-when cannot be verified by the agent (needs Windows+Office Interop), so build correctness must be argued statically. Estimate assumes specification.md §4 gives a clear template contract to document against."}
